export const metadata = {
  title: "YAIRINK — Administración",
  description: "Panel de administración del estudio YAIRINK",
};

export default function AdminRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
